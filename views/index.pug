extends layout

block content
  if user
    h1 #{title}
    ul.list-group
      each user, i in users
        li.list-group-item
          a(href='/users/'+user._id)= user.username
  else
    h2 Ingresa como administrador... 🔐
